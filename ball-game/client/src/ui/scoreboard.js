// client/src/ui/scoreboard.js
// Placeholder for future global stats (minimal in V1).

export class ScoreboardUI {
  constructor() {
    this.data = null;
  }

  update(data) {
    // V1 has no visible scoreboard (global anonymous session).
    // Keeping structure for future expansion.
    this.data = data;
  }
}
